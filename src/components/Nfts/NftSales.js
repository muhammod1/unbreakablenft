import React from "react";
import { motion } from "framer-motion";
import LinkCard from "../UI/LinkCard";
const NftSales = () => {
  return (
    <LinkCard text="Buy an " subtext="Unbroken" desc="The initial sale has sold out. To get your Unbroken NFT, check out the collection on OpenSea." linktext="View on opensea" url="#"  />
  );
};

export default NftSales;
